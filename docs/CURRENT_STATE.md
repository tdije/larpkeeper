# Current State

- CLI name: `Larpkeeper`
- aliases: `larpkeeper`, `larp`, `lorekeeper`, `lore`
- legacy compatibility alias: `context-gardener`
- main purpose: keep context packs small and useful
- active profiles: DripTech AI Studio, Metis, radio-engine

Current risks:

- classifier still relies on path heuristics
- Graphiti adapter is lightweight
- shell help is good, but runtime can still drift

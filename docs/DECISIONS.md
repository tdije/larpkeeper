# Decisions

- Keep `context-gardener` as a compatibility alias.
- Use `larp` as the short public command.
- Keep Graphiti append gated and durable-only.
- Treat profiles as boot hints, not source of truth.
